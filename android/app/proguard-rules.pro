# App-specific R8/ProGuard keep rules. Flutter's own rules are injected by the
# Flutter Gradle plugin; add rules here only when a release build or runtime
# verification demonstrates they are needed.
